# message_spam_auto
Automatic scheduled message sender for Telegram chats using Python and Telethon.   Set your `api_id`, `api_hash`, phone number, `chat_id`, and message list.   The script sends each message with a short delay and then waits a random interval before the next cycle.   Great for automating notifications, reminders, or routine messages in any chat.
